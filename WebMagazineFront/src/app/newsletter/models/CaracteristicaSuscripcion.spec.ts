import { CaracteristicaSuscripcion } from './CaracteristicaSuscripcion';

describe('CaracteristicaSuscripcion', () => {
  it('should create an instance', () => {
    expect(new CaracteristicaSuscripcion()).toBeTruthy();
  });
});
